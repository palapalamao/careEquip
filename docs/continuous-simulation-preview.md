# 设备管理持续模拟任务：写入预览

状态：已创建并启用。首轮验收发现作业只完成了第一个点：异步历史写入没有在作业结束前等待完成。第一版修复使用的等待调用不属于 Axon 作业表达式，未产生写入。第二版修复已通过本机 FIN Axon 语法检查并由 FIN Expert 暂存，尚未写入。

## 执行目标与效果

- 连接：`local-mytest`；项目：`mytest`；FIN：`5.3.0.2761`。
- 新建一个原生 FIN Jobs 作业：**设备管理模拟数据持续补充**，记录 ID `dm-demo-telemetry-job`。
- 周期：`every 1min`，持续运行，直到在 Jobs 中禁用。
- 固定处理现有 48 个 `deviceManager-demo-v1` 模拟点及其设备；每次先检查全部记录的模拟标记和历史配置。
- 每分钟为各点生成不同相位的平滑波动值，更新 `curVal`、`curStatus` 和设备页面使用的数值、质量、更新时间；向历史追加同一时间的新样本。
- 每天约新增 69,120 个历史样本。历史保留设置沿用现有配置。
- 已有历史不改写。停机期间不生成样本；FIN 恢复后由原生调度继续运行。
- 本次不需要更新 POD 或重启 FIN。该任务在 FIN 中独立运行，不依赖此对话保持打开。

## 范围与停止方式

新建一条作业记录，周期性追加上述模拟点的历史并更新瞬时值。不得操作真实设备、连接器、控制输出或其他项目记录。记录所属数据集或标记不符时，作业报错并停止该轮执行。

在 FIN 的 Jobs 中找到 **设备管理模拟数据持续补充** 并禁用，可停止后续补充；已经生成的历史会保留。当前页面趋势在重新加载时读取最新历史，本任务不修改页面自动刷新行为。

## 已执行的创建

- FIN Expert 执行审计 ID：`9d1a1f77-e3ca-4ffd-ba9b-1024cd3b11c8`。
- 已创建记录：`dm-demo-telemetry-job`，调度为 `every 1min`。
- 实测：第一个模拟点历史从 97 增至 103，最后一个模拟点仍为 97，说明不能把当前作业视为有效完成。

## 修复写入预览

- FIN Expert 状态：`staged`，尚未向 FIN 发出第二版修复写入。
- 风险分类：`high_impact`；分类器将 `if` 识别为未知调用，且该操作会更新持续写入作业。
- 请求 SHA-256：`2db9ccfb775642460e41580c2fb8a20fd4639e09b4b8c1afe19d4dee064ad7aa`
- 预览审计 ID：`98dc4cf1-9aac-4e9b-bbb4-a363033c490e`
- 本次令牌到期时间：**2026-09-09 15:23:57 +08:00**。令牌不写入本说明；若到期，需对同一请求重新暂存。
- 精确修复内容：`../output/fin-import-ongoing-update.axon`
- 每轮执行内容：`../output/fin-import-ongoing-tick.axon`

修复仅在每个 `hisWrite` 后加入 FIN 原生作业的 250 毫秒让出时间，其他点位、周期、标记和禁用方式均不变。确认后执行修复，并观察至少两个调度周期：检查作业状态、48 个非空当前值，以及实际历史时间和条数增长。语法检查和作业状态本身不代表历史已成功落盘。

## 当前故障处置预览

第二版修复后，48 个点中仍只有第一个点增长；作业不能继续以这种状态运行。以下两项均已暂存，尚未执行：

1. 停用 `dm-demo-telemetry-job`，停止继续产生不完整的首点历史。
   - 请求 SHA-256：`ee3378df6e52c65ef9603c0c6a1c3a13a55e22c89ca1342c8af2d143a3874a8e`
   - 审计 ID：`1af38fed-5312-4d0c-9102-32196fd61341`
   - 到期：**2026-09-09 15:29:49 +08:00**。
2. 读取该 FIN 原生作业的运行日志，仅用于取得准确错误原因，不写入任何数据。
   - 请求 SHA-256：`1bc14b78feafea165393c569c7209267da69ce430ad1ea8e018d79ade5e1660c`
   - 审计 ID：`28381616-3744-4c4b-96ce-c74d49895365`
   - 到期：**2026-09-09 15:29:53 +08:00**。

FIN Expert 将日志查询按高影响未知调用暂存；实际作用是只读。确认后先停用作业，再读取日志，基于错误做一次有依据的修复。

## 日志句柄补充读取预览

上述日志操作已执行，且故障作业已停用；FIN 返回“记录 ID 不是运行句柄”。为取得该句柄，需读取 FIN 当前作业状态列表，操作只读，不写入任何记录：

- 请求：`jobStatusAll()`；请求 SHA-256：`f390e6a897b612563211ba86f45538de2b97bd13d6e94589315bef43f59f90d4`
- 审计 ID：`e45a8d40-0f31-4499-826b-22d448d58797`
- 到期：**2026-09-09 15:36:06 +08:00**。
- FIN Expert 将该官方只读函数分类为未知调用并要求确认；执行后只会读取句柄，不会恢复或修改作业。

## 根因修复与重新启用预览

作业状态读取已确认根因：`dmValue` 是持久标签，却被以 transient 方式提交，FIN 在第一个点后报 `Cannot update persistent tag transiently: dmValue`。故障作业现已正确带有 `disabled` 标记，不会继续运行。

本次更新会在同一受保护提交中移除 `disabled` 并写入修复表达式：

- `curVal`、`curStatus` 单独以 transient 提交；
- `dmValue`、质量和更新时间分别作为持久页面快照提交；
- 每点历史写入后让出 250 毫秒，再处理下一点；
- 48 点、每分钟周期、模拟数据集和禁止物理写入的边界保持不变。

FIN Expert 已暂存，尚未重新启用：

- 请求 SHA-256：`e224138e7ba64e09c0fb35a5b11019c18ba9ce346bca227ad8f672b1158720cf`
- 审计 ID：`b4e36d4d-e8c0-47e3-81b1-e1c19d612144`
- 到期：**2026-09-09 15:54:07 +08:00**。

确认后会重新启用该作业，并连续验证至少两个周期的全部 48 点历史增长和 `doneOk` 状态。

离线审查限制：FIN Expert 按函数 Trio 模板检查独立作业表达式，报告缺少 `name/func/doc/src`；本任务采用原生 `jobExpr`，不注册函数，因此这些模板字段不适用。FIN Expert 对目标版本的适配状态为 `unverified`，不宣称其审查通过；当前通过的是本机 FIN 5.3 Axon 解析检查，实际运行待上述验收。
